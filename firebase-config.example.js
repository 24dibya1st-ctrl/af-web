/**
 * Local Firebase web app config (optional).
 *
 * 1. Copy this file to firebase-config.js (same folder).
 * 2. Paste values from Firebase Console → Project settings → Your apps → Web app.
 *
 * firebase-config.js is gitignored and not deployed — safe for localhost testing.
 */
export const firebaseWebConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};
